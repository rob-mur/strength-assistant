# Component Inventory

_This document is a placeholder generated during a "quick" scan. A "deep" or "exhaustive" scan is required to populate this with a detailed inventory of UI components._

## Overview

The application's UI components are primarily located in the `lib/components` directory. Screens, which are composed of these components, are located in `app/(tabs)`.

The project uses **React Native Paper** as a base component library.

## High-Level Categories (Inferred)

-   **Form Components**: Inputs, buttons, pickers for user data entry.
-   **Layout Components**: Cards, lists, and screen containers.
-   **Display Components**: Components for showing exercise data, workout logs, etc.
-   **Navigation Components**: Tabs and headers, managed by Expo Router and React Navigation.
