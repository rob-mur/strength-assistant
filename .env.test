# Test environment configuration
# Ensure Firebase is used for Chrome integration tests
USE_SUPABASE_DATA=false
EXPO_PUBLIC_USE_SUPABASE_DATA=false
# Fix Expo Router app root for web builds
EXPO_ROUTER_APP_ROOT=./app