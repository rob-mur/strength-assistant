name: Deploy Web Application

on:
  workflow_call:
    inputs:
      environment:
        description: "Deployment environment (production or preview)"
        required: true
        type: string
      pr_number:
        description: "Pull request number for preview deployments"
        required: false
        type: string
    outputs:
      deployment_url:
        description: "URL of the deployed web application"
        value: ${{ jobs.deploy.outputs.deployment_url }}
      deployment_status:
        description: "Status of the deployment"
        value: ${{ jobs.deploy.outputs.deployment_status }}

env:
  NODE_ENV: production

jobs:
  deploy:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment == 'production' && 'Dev' || 'preview' }}
    outputs:
      deployment_url: ${{ steps.deploy.outputs.deployment_url }}
      deployment_status: ${{ steps.deploy.outputs.deployment_status }}

    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔑 Setup EAS Environment Variables
        id: eas-env
        run: |
          echo "Setting up EAS environment variables for web deployment..."
          
          # Determine environment based on input
          if [ "${{ inputs.environment }}" = "production" ]; then
            EAS_ENVIRONMENT="production"
          else
            EAS_ENVIRONMENT="preview"
          fi
          
          echo "Using EAS environment: $EAS_ENVIRONMENT"
          
          # Pull environment variables from EAS (this creates a .env file)
          echo "Pulling environment variables from EAS..."
          npx eas-cli env:pull --environment=$EAS_ENVIRONMENT
          
          # Verify .env file was created and contains required variables
          if [ ! -f .env ]; then
            echo "❌ EAS env:pull did not create .env file"
            echo "Available files:"
            ls -la
            exit 1
          fi
          
          echo "Environment variables pulled from EAS:"
          grep "EXPO_PUBLIC_" .env || echo "No EXPO_PUBLIC_ variables found"
          
          # Source the environment file to make variables available
          source .env
          
          # Verify required variables are set
          if [ -z "$EXPO_PUBLIC_SUPABASE_URL" ] || [ -z "$EXPO_PUBLIC_SUPABASE_ANON_KEY" ]; then
            echo "❌ Missing required Supabase environment variables"
            echo "EXPO_PUBLIC_SUPABASE_URL: ${EXPO_PUBLIC_SUPABASE_URL:-'not set'}"
            echo "EXPO_PUBLIC_SUPABASE_ANON_KEY: ${EXPO_PUBLIC_SUPABASE_ANON_KEY:0:20}..."
            echo "All EXPO_PUBLIC_ variables:"
            env | grep EXPO_PUBLIC_ || echo "None found"
            exit 1
          fi
          
          echo "✅ Successfully configured EAS environment variables"
          echo "SUPABASE_URL=${EXPO_PUBLIC_SUPABASE_URL}" >> $GITHUB_OUTPUT
          echo "SUPABASE_ANON_KEY=${EXPO_PUBLIC_SUPABASE_ANON_KEY}" >> $GITHUB_OUTPUT
          
          # Add environment variables to GitHub environment for subsequent steps
          echo "EXPO_PUBLIC_SUPABASE_URL=${EXPO_PUBLIC_SUPABASE_URL}" >> $GITHUB_ENV
          echo "EXPO_PUBLIC_SUPABASE_ANON_KEY=${EXPO_PUBLIC_SUPABASE_ANON_KEY}" >> $GITHUB_ENV
          echo "EXPO_PUBLIC_ENVIRONMENT=${{ inputs.environment }}" >> $GITHUB_ENV
          echo "EXPO_PUBLIC_PR_NUMBER=${{ inputs.pr_number || '' }}" >> $GITHUB_ENV
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: 🔍 Verify Expo web build capability
        run: |
          echo "Checking if expo export command is available..."
          npx expo --version
          echo "Checking package.json for web build script..."
          npm run build_web --dry-run || echo "Using direct expo export command"

      - name: 🌐 Build Expo Web
        run: |
          echo "Building web application using EAS-managed environment variables..."
          
          echo "Environment check:"
          echo "SUPABASE_URL: ${EXPO_PUBLIC_SUPABASE_URL:0:50}..."
          echo "SUPABASE_KEY: ${EXPO_PUBLIC_SUPABASE_ANON_KEY:0:20}..."
          echo "ENVIRONMENT: ${EXPO_PUBLIC_ENVIRONMENT}"
          echo "PR_NUMBER: ${EXPO_PUBLIC_PR_NUMBER}"
          
          # Verify .env file exists (created by EAS env:pull)
          if [ -f .env ]; then
            echo "Using .env file from EAS:"
            cat .env | grep -v "ANON_KEY" || true  # Show env vars but hide anon key
          fi
          
          # Build with EAS environment variables
          echo "Building with: npx expo export -p web"
          npx expo export -p web
          
          echo "Build completed. Checking output..."
          ls -la dist/
          test -f dist/index.html || (echo "ERROR: index.html not found in dist/" && exit 1)
          
          # Verify environment variables are embedded in the build
          if grep -q "$EXPO_PUBLIC_SUPABASE_URL" dist/_expo/static/js/web/*.js; then
            echo "✅ Supabase URL successfully embedded in build"
          else
            echo "⚠️ Warning: Supabase URL might not be embedded in build"
          fi

      - name: 🚀 Deploy to Vercel
        id: deploy
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ./
          vercel-args: ${{ inputs.environment == 'production' && '--prod' || '' }}
        # No additional env needed here - already baked into build

      - name: 📝 Set deployment outputs
        run: |
          echo "deployment_url=${{ steps.deploy.outputs.preview-url }}" >> $GITHUB_OUTPUT
          echo "deployment_status=success" >> $GITHUB_OUTPUT
          echo "supabase_url=${{ steps.eas-env.outputs.SUPABASE_URL }}" >> $GITHUB_OUTPUT
        shell: bash

      - name: ✅ Deployment Success
        run: |
          echo "🎉 Web deployment completed successfully!"
          echo "📱 Deployment URL: ${{ steps.deploy.outputs.preview-url }}"
          echo "🌍 Environment: ${{ inputs.environment }}"

      - name: ❌ Deployment Failure
        if: failure()
        run: |
          echo "💥 Web deployment failed!"
          echo "📋 Check the logs above for detailed error information"
          echo "🔧 Common issues:"
          echo "  - Missing environment variables"
          echo "  - Build command failure"
          echo "  - Vercel authentication issues"
          exit 1
